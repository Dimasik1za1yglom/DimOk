server:
  port: 0

spring:
  application:
    name: accountServer
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/social_network
    username: postgres
    password: "112348Diman"
  liquibase:
    change-log: classpath:db/changelog/db.changelog-root.xml
    enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      jakarta:
        persistence:
          sharedCache:
            mode: UNSPECIFIED
  kafka:
    producer:
      bootstrap-servers: "127.0.0.1:9092"
      client-id: "producer"
      properties:
        spring:
          json:
            add:
              type:
                headers: false
      value-serializer: org.springframework.kafka.support.serializer.JsonDeserializer


application:
  kafka:
    topic: "alertUser"

eureka:
  instance:
    hostname: localhost
    instance-id: ${spring.application.name}:${random.uuid}
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:8081/eureka/

logging:
  logback:
    rollingpolicy:
      clean-history-on-start: false
      file-name-pattern: logs/${spring.application.name}/${spring.application.name}-%d{yyyy-MM-dd}.%i.log
      max-file-size: 5MB
  file:
    name: logs/${spring.application.name}-today.log
  level:
    org:
    hibernate:
      sql: info

jwt:
  secret:
    refresh: oqPXs3V5eMDrJw8/rrryMqiqMjuKrVe5HJY7CXu5GK5f3DEKRXpFEl8S6SGLd44N3jtAC8xDPGbu49eyp8w9xA==
